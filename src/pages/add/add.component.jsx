import "./add.css";

import Form from "../../components/add/form/form.component";

const AddPage = (props) => {
  return (
    <div className="head">
      <Form />
    </div>
  );
};

export default AddPage;
